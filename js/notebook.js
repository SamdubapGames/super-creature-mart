// ============================================================
// notebook.js
// 담당: 헤
// 역할: 공책 UI에 표시할 데이터를 만드는 함수들
//
// 규칙:
//   - 함수 이름, 파라미터 이름 바꾸지 말 것
//   - 함수 안의 로직만 채울 것
//   - console.log로 중간 확인해도 됨
// ============================================================


// ============================================================
// Function 1: 팁 한 줄 만들기
// ============================================================
// 부위 이름과 아이템 이름을 받아서
// "입 → 떡" 형태의 문자열 한 줄을 반환합니다.
//
// 인풋(파라미터):
// - part: 문자열. 부위 이름.
//   → "입"
// - item: 문자열. 아이템 이름.
//   → "떡"
//
// 리턴(반환값):
// - 문자열(string). "부위 → 아이템" 형태.
//   → formatTipLine("입", "떡")   는 "입 → 떡"
//   → formatTipLine("코", "캣닙 담배") 는 "코 → 캣닙 담배"

function formatTipLine(part, item) {
    // 기능: part 와 item 을 "→" 로 이어붙인 한 줄 문자열 반환
    // input(parameter): part(string), item(string)
    // return값 타입: string
    // 여기를 채우세요
}

// ✅ 함수 완성 후 아래 주석을 풀어서 확인해보세요
/*
console.log(formatTipLine("입", "떡"));          // "입 → 떡"
console.log(formatTipLine("코", "캣닙 담배"));   // "코 → 캣닙 담배"
console.log(formatTipLine("눈", "레몬"));        // "눈 → 레몬"
*/


// ============================================================
// Function 2: 공책 전체 표시
// ============================================================
// 부위 배열과 아이템 배열을 받아서
// 전체 팁 목록을 여러 줄 문자열로 만들어 반환합니다.
//
// 인풋(파라미터):
// - parts: 배열(string[]). 부위 이름들.
//   → ["입", "꼬리", "코", "눈"]
// - items: 배열(string[]). 아이템 이름들.
//   → ["떡", "칼", "캣닙 담배", "레몬"]
//   → parts[i] 와 items[i] 는 항상 한 쌍입니다.
//
// 리턴(반환값):
// - 문자열(string). 줄바꿈(\n)으로 이어진 전체 목록.
//   → "입 → 떡\n꼬리 → 칼\n코 → 캣닙 담배\n눈 → 레몬"
//
// 참고: \n 은 줄바꿈 문자입니다.
//       console.log 로 출력하면 실제로 줄이 바뀌어서 보입니다.
//       줄을 이어붙일 때:
//         첫 번째 줄은 앞에 \n 없이
//         두 번째 줄부터 앞에 \n 붙이기
//       또는 배열에 push 하고 마지막에 join("\n") 써도 됩니다.

function showNotebook(parts, items) {
    // 기능: parts 와 items 를 짝지어 전체 팁 목록 문자열 반환
    // input(parameter): parts(배열), items(배열)
    // return값 타입: string
    // 꼭 써야 하는 것:
    //   for 문으로 두 배열 동시 순회 (parts[i], items[i])
    //   formatTipLine(parts[i], items[i]) 호출해서 한 줄 얻기
    //   줄들을 \n 으로 이어붙이기
    //   여기를 채우세요
}

// ✅ 함수 완성 후 아래 주석을 풀어서 확인해보세요
/*
var PARTS = ["입", "꼬리", "코", "눈"];
var ITEMS = ["떡", "칼", "캣닙 담배", "레몬"];

console.log(showNotebook(PARTS, ITEMS));
// 입 → 떡
// 꼬리 → 칼
// 코 → 캣닙 담배
// 눈 → 레몬

console.log(showNotebook(["눈"], ["레몬"]));
// 눈 → 레몬
*/
